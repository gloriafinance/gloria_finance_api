<div style="width: 100%; margin: 0 auto; text-align: left;">
  <p>
    Concluímos o processamento do inventário físico enviado em
    <strong>{{startedAt}}</strong>.
  </p>

  <div style="margin: 16px 0;">
    {{#if church.name}}
      <p>
        <strong>Congregação:</strong>
        {{church.name}}
      </p>
    {{/if}}
    {{#if performer.name}}
      <p>
        <strong>Responsável:</strong>
        {{performer.name}}
        {{#if performer.email}} ({{performer.email}}) {{/if}}
      </p>
    {{/if}}
    {{#if finishedAt}}
      <p>
        <strong>Finalizado em:</strong>
        {{finishedAt}}
      </p>
    {{/if}}
  </div>

  <h2 style="font-size: 18px; margin: 24px 0 12px;">Resumo do processamento</h2>
  <table
    style="width: 100%; border-collapse: collapse; font-size: 14px; margin-bottom: 24px;"
  >
    <thead>
      <tr>
        <th
          style="text-align: left; padding: 8px; border-bottom: 1px solid #e5e7eb;"
        >
          Métrica
        </th>
        <th
          style="text-align: right; padding: 8px; border-bottom: 1px solid #e5e7eb;"
        >
          Quantidade
        </th>
      </tr>
    </thead>
    <tbody>
      <tr>
        <td style="padding: 8px; border-bottom: 1px solid #f3f4f6;">
          Linhas processadas
        </td>
        <td
          style="padding: 8px; text-align: right; border-bottom: 1px solid #f3f4f6;"
        >
          {{processed}}
        </td>
      </tr>
      <tr>
        <td style="padding: 8px; border-bottom: 1px solid #f3f4f6;">
          Ativos atualizados
        </td>
        <td
          style="padding: 8px; text-align: right; border-bottom: 1px solid #f3f4f6;"
        >
          {{updated}}
        </td>
      </tr>
      <tr>
        <td style="padding: 8px; border-bottom: 1px solid #f3f4f6;">
          Linhas ignoradas
        </td>
        <td
          style="padding: 8px; text-align: right; border-bottom: 1px solid #f3f4f6;"
        >
          {{skipped.length}}
        </td>
      </tr>
      <tr>
        <td style="padding: 8px;">Falhas</td>
        <td style="padding: 8px; text-align: right;">
          {{errors.length}}
        </td>
      </tr>
    </tbody>
  </table>

  {{#if skipped.length}}
    <div style="margin-bottom: 24px;">
      <h3 style="font-size: 16px; margin-bottom: 8px;">
        Linhas ignoradas
      </h3>
      <p style="font-size: 14px; margin-bottom: 8px;">
        Estas linhas foram desconsideradas por falta de informações
        obrigatórias:
      </p>
      <ul style="padding-left: 20px; font-size: 14px;">
        {{#each skipped}}
          <li>
            {{#if assetId}}
              <strong>{{assetId}}:</strong>
            {{/if}}
            {{reason}}
          </li>
        {{/each}}
      </ul>
    </div>
  {{/if}}

  {{#if errors.length}}
    <div style="margin-bottom: 24px;">
      <h3 style="font-size: 16px; margin-bottom: 8px;">Falhas encontradas</h3>
      <p style="font-size: 14px; margin-bottom: 8px;">
        As linhas abaixo não puderam ser aplicadas. Revise os dados e tente
        novamente:
      </p>
      <ul style="padding-left: 20px; font-size: 14px;">
        {{#each errors}}
          <li>
            {{#if assetId}}
              <strong>{{assetId}}:</strong>
            {{/if}}
            {{reason}}
          </li>
        {{/each}}
      </ul>
    </div>
  {{/if}}

  <p style="font-size: 14px; margin-top: 24px;">
    Se precisar reenviar o arquivo, basta corrigir os pontos listados nas seções
    acima e repetir o procedimento no módulo Patrimônio.
  </p>
</div>
